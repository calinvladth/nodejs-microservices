services:
  ui-pick-on-maps:
    container_name: ui-pick-on-maps
    build:
      context: ./ui/pick-on-maps
      target: prod
    environment:
      - NODE_ENV=production
      - VITE_API_URL=$API_URL
    ports:
      - '8080:8080'
    volumes:
      - ./ui/pick-on-maps/src:/usr/src/app/src
      - /usr/src/app/src/node_modules